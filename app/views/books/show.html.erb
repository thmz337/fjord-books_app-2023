<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('books.defaults.to_edit'), edit_book_path(@book) %> |
  <%= link_to t('books.defaults.to_index'), books_path %>

  <%= button_to t('books.defaults.to_delete'), @book, method: :delete %>
</nav>
